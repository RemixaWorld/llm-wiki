---
domain: medium.com
fetch_date: '2026-05-18T12:51:37.357927'
status: ok
url: https://medium.com/@techsachin/spar-self-play-with-tree-search-refinement-to-improve-instruction-following-in-llms-3047c5ac7735
---

# SPAR : Self-Play framework with Tree-Search Refinement to improve Instruction-Following in LLMs

[ ![SACHIN KUMAR](https://miro.medium.com/v2/resize:fill:64:64/1*7GE5_sjWH8e95wFj2s9sgg.jpeg) ](</@techsachin?source=post_page---byline--3047c5ac7735--------------------------------------->)

[SACHIN KUMAR](</@techsachin?source=post_page---byline--3047c5ac7735--------------------------------------->)

8 min read

·

Dec 17, 2024

\--

\--

Listen

Share

More

Instruction-following capabilities in LLMs requires model to recognize even the most subtle requirements in the instructions and accurately reflect them in its output. Existing methods optimize it via preference learning, where those methods often directly sample multiple independent responses from the model when creating preference pairs. But those methods can introduce content variations irrelevant to whether the instruction is precisely followed (e.g., different expressions about the same semantic), interfering with the goal of teaching models to recognize the key differences that lead to improved instruction following.

To address it, this paper[1] introduce SPAR, a self-play framework integrating tree-search self-refinement to yield valid and comparable preference pairs free from distractions. By playing against itself, an LLM employs a tree-search strategy to refine its previous responses with respect to the instruction while minimizing unnecessary variations.

**Key contributions:**

* reveal that preference pairs derived from independently sampled responses often contain interfering factors, hampering preference learning to improve instruction following
* introduce SPAR, a novel self-play framework that enables continuous self-improvement in instruction-following tasks
* construct a high-quality dataset with 43K complex instruction-following prompts and an SFT dataset that can improve the instruction-following capabilities of LLMs

## Method

### i) Overall Framework

SPAR iterative training framework is illustrated in figure below

![image](https://miro.medium.com/v2/resize:fit:700/1*VfBDsAgsqFgItxTSixl0gg.png)

* Formally, in each iteration, given an instruction x from the prompt set, the actor generates a response y.
* The refiner identifies the responses that do not follow the instructions accurately, termed as negative responses.
* objective is to refine the negative response into a correct response
* These generated refinement pairs, are collected and used to optimize the actor via Direct Preference Optimization (DPO)
* Simultaneously, applied Rejection-sampling Fine-Tuning (RFT) to improve the refiner. This process prepares both models for the next iteration of self-improvement

### ii) Data Construction

**a) Prompt Creation**

* Seed Prompts: use a seed set of prompts derived from the Infinity-Instruct dataset, which contains ten million high-quality conversations. After applying rule-based filtering based on length, keywords, and self-BLEU, we obtain approximately 50k seed prompts.
* Taxonomy-based Prompt Construction: adopt a taxonomy-based mechanism to make constraint types comprehensive and balanced. After building the constraint taxonomy, we employ it to construct complex instruction-following tasks based on seed prompts. It results in about 43k prompts

**b) Actor and Refiner Initialization**

* **Actor Data Creation:** first collect a strong LLM’s responses to these complex prompts, thereby producing supervised fine-tuning (SFT) data (x, y) ∈ DActor for the actor model, where x is the complex instruction and y is the strong LLM’s response. Then, we fine-tune the base model to get an initial actor M0
* **Refiner Data Creation:** To bootstrap the refiner model with strong judgment and refinement capability, we sample responses from the initial actor M0. Then, we collect the judgments from a strong LLM to form a dataset, (x, y, j) ∈ DJSFT. We collect responses that are judged not to accurately follow instructions and term them as negative responses. For these negative responses, we use the strong LLM to correct them with minimal revisions to avoid irrelevant variations
* **Training Strategy:** For both actor and refiner models, we use standard supervised fine-tuning with the loss function:

![image](https://miro.medium.com/v2/resize:fit:312/1*sY3-HI5rTDqlk3FsyGXUXw.png)

where q denotes the input, r signifies the target response, and N represents the length of r. For actor training, we have input q = x and target r = y. When it comes to the refiner, we use input q = (x, y) and target r = j for DJSFT, and input q = (x, ynegative, j) and target r = yrefined for DRSFT.

### iii) Tree-Search Integrated Self-Play Training

**a) Negative Data Collection**

* For each prompt x, we first sample K responses {y1, y2, . . . , yK} from the actor model
* Then, for each prompt and response pair, we utilize the refiner to generate a judgment, which contains two parts: a label suggesting whether the response follows the instruction and an explanation about the assessment.
* To make this judgment more accurate, we incorporate the self-consistency mechanism, by obtaining multiple judgments from the refiner and determine the final label through majority voting.
* After majority voting, we randomly select one judgment that matches the voted label to serve as the final judgment, which allows to identify challenging prompts that elicit responses that do not accurately follow the instructions, yielding tuples in the form of (x, ynegative, j), where ynegative is the incorrect response and j is its corresponding judgment

**b) Tree-Search Refinement**

* Given that direct refinement often results in a low success rate, employed a tree-search approach, with implementation of both breadth-first search (BFS) and depth-first search (DFS) strategies for this refinement.
* With BFS as an example, Starting with an incorrect instruction-response pair and its judgment as the root node, we expand the search tree level-by-level until a correct response is found.
* At each intermediate node, we generate potential refinements for the current response and evaluate its correctness using the refiner. The number of generated refinements corresponds to the number of branches
* Specifically, at a level of the tree, the refiner: 1). generates potential refinements for each node in the current level; 2). judges the correctness of these refinements. This creates a set of child nodes with new responses and their corresponding judgments.
* search process continues until we obtain a tuple (x, ynegative, yrefined), where yrefined is the newly refined, correct response

**c) Actor Training**

* leverage the refinement pairs for preference learning using DPO, for optimizing actor model
* At iteration t, we train the actor model Mt with refinement pairs (ynegative, yrefined), treating ynegative as the rejected response (yl) and yrefined as the chosen response (yw).
* The training dataset is denoted as Dtdpo and the DPO loss is described as follows:

![image](https://miro.medium.com/v2/resize:fit:700/1*w1PlYgURzaze5tU-fd0HQQ.png)

where πtθ represents the actor model Mt, and the reference model πref initialized with Mt remains fixed during the training process. This results in a new actor model, Mt+1, for the next iteration

**d) Refiner Training**

Given that the input for the refiner is templated, we use Rejection-sampling Fine-Tuning (RFT) to obtain the new refiner Rt+1. The RFT training data consists of two components:

**(1) Refinement Training Data**

* refinement training data consists of tuples that capture the process of refining incorrect responses.
* For each incorrect response from the tree-search based refinement step, we collect tuples in the form of (x, yp, jp, yrefined), where (x, yp, jp) represents the parent node of the final correct response in the refinement tree, and yrefined is the correctly refined response

**(2) Judgment Training Data**

* judgment training data is derived both from the negative data collection and nodes of the tree-search process.
* This dataset consists of tuples (x, yi, ji), where x is the prompt, yi is a response to x, and ji is the judgment consistent with majority voting.
* Then, we perform supervised fine-tuning using the constructed training data.
* For the refinement data Dtrefine we use the tuples (x, yp, jp, yrefined) with input q = (x, yp, jp) and target r = yrefined. For the judgment data Dt judge, we use the tuples (x, yi, ji) with input q = (x, yi) and target r = ji.

## Experiments

### i) Actor Evaluation results

**a) SPAR significantly improves instruction-following ability**

* Table below shows main results of iteratively trained LLMs on instruction-following benchmarks

![image](https://miro.medium.com/v2/resize:fit:700/1*swCUS2E_TAutxVqwUiJT9w.png)
* after three training iterations, SPAR-8B-DPO-iter3 even surpasses GPT-4-Turbo (81.3% average accuracy) on IFEval. Moreover, incorporating the tree-search refinement technique during the inference stage significantly boosts performance.
* Additionally, the SPAR showcases excellent scalability with respect to model size, which substantially enhances the instruction-following abilities of the LLaMA3–70B-Instruct model.

**b) SPAR does not damage general abilities**

* Table below shows performance on general benchmarks

![image](https://miro.medium.com/v2/resize:fit:700/1*bHgXLjtcNHR52GCe3OCTcg.png)
* results indicate that SPAR maintains or even improves general performance, particularly on GSM8k and HumanEval benchmarks, demonstrating that enhanced instruction-following capabilities support overall LLM alignment

**c) SPAR outperforms other baselines significantly**

* Figure below demonstrates the improvements on IFEval with each training iteration

![image](https://miro.medium.com/v2/resize:fit:498/1*IGpvVfEpwfLXPSjviNls1g.png)
* In every iteration, SPAR outperforms other methods. Notably, even after three iterations, other methods fail to surpass the performance of SPAR’s first iteration.

### ii) Refiner Evaluation results

**a) SPAR iteratively enhances judgment capability**

* Table below shows Evaluation of judgment capability for iteratively trained LLMs on LLMBar.

![image](https://miro.medium.com/v2/resize:fit:700/1*5-vGdL40Mh4qJMc9wC0VUA.png)
* results shows that SPAR iterations notably improve the model’s ability to evaluate instruction-following tasks
* By iteration three, the refiner SPAR-8B-RFT-iter3 surpasses GPT-4o-Mini, the model used to construct the judgment SFT dataset

**b) SPAR progressively improves refinement capability**

* Table below shows Refinement evaluation results.Acc-GPT uses GPT-4o as judge; Acc-SPAR uses SPAR-8B-RFT-iter3

![image](https://miro.medium.com/v2/resize:fit:406/1*hxU900EbnXVbRj9lGHyU1A.png)
* results demonstrates continuous improvement in refinement accuracy (success rate) of LLaMA3–8B-Instruct with each training iteration, eventually matching the level of GPT-4o-Mini, the strong LLM for SFT data construction

## Conclusion

* introduce a new self-play framework, SPAR, designed to improve the instruction-following capabilities of LLMs through training with refinement pairs
* reveal that, unlike traditional approaches that rely on sampling multiple independent responses from the model to construct preference pairs, refining preference pairs to minimize extraneous factors and highlight key differences lead to significant improvements in instruction-following tasks
* LLaMA3-8B-Instruct model, trained iteratively using our framework, outperforms GPT-4-Turbo on IFEval.
* With inference time compute scaling, its performance can be further improved.

Paper: <https://arxiv.org/abs/2412.11605>

Code: <https://github.com/thu-coai/SPaR>

References:

1. SPaR: Self-Play with Tree-Search Refinement to Improve Instruction-Following in Large Language Models by Cheng et al.[arXiv:2412.11605](<https://arxiv.org/abs/2412.11605>)
