---
domain: medium.com
fetch_date: '2026-05-18T12:51:05.076836'
status: ok
url: https://medium.com/data-science-in-your-pocket/pydanticai-pydantic-ai-agent-framework-for-llms-6c60c86e0e48
---

# PydanticAI: Pydantic AI Agent Framework for LLMs

##  _Agent Framework to use Pydantic with LLMs_

[ ![Mehul Gupta](https://miro.medium.com/v2/resize:fill:64:64/1*vyvhK_h4zA05mg_Y-n2qBA.jpeg) ](</@mehulgupta_7991?source=post_page---byline--6c60c86e0e48--------------------------------------->)

[Mehul Gupta](</@mehulgupta_7991?source=post_page---byline--6c60c86e0e48--------------------------------------->)

5 min read

·

Dec 6, 2024

\--

\--

Listen

Share

More

![Photo by julien Tromeur on Unsplash](https://miro.medium.com/v2/resize:fit:700/0*EFZ6IPu-Xllkdnth)

The Generative AI space is among the fastest-growing fields in mankind's history. As you might be hearing, 2025 will be the year of AI Agents/multi-AI agents, shifting from solely LLMs. The current developments show the same trend: big giants like AWS, OpenAI, Microsoft, and others have been releasing AI Agent frameworks one after the other.

PydanticAI demo app

Of all the releases so far, PydanticAI agentic framework appears to be the most useful and important release due to its support for Pydantic with LLMs

## [Subscribe to datasciencepocket on GumroadOn a Mission to teach AI to everyone !datasciencepocket.gumroad.com](<https://datasciencepocket.gumroad.com/?source=post_page-----6c60c86e0e48--------------------------------------->)

Before we understand why PydanticAI is a crucial framework, we must understand

## What is Pydantic?

**Pydantic** is a Python library used to validate and parse data easily. It helps ensure your data is accurate and follows the expected structure. It’s especially useful when dealing with external inputs like JSON files, user data, or API responses.

Instead of manually writing checks for each field (e.g., “Is this an integer? Is this string too long?”), Pydantic automates these checks by using **models**.

Suppose you’re making an app where users submit their name, age, and email. You want to ensure:

> The name is a string.
>
> The age is a number.
>
> The email is in a valid format.

Here’s how Pydantic makes this easy:

```python from pydantic import BaseModel, EmailStr# Define the modelclass User(BaseModel): name: str age: int email: EmailStr# Example inputuser_data = { "name": "Alice", "age": 25, "email": "alice@example.com"}# Validate the inputuser = User(**user_data)print(user.name) # Aliceprint(user.age) # 25print(user.email) # alice@example.com ```

### What if the data is wrong?

If the user submits invalid data (e.g., `"age": "twenty-five"`), Pydantic will throw an error automatically:

```json user_data = { "name": "Alice", "age": "twenty-five", # Invalid "email": "alice@example.com"}user = User(**user_data)# Error: value is not a valid integer ```

Pydantic plays a critical role in deployment as is mostly mandatory to follow because

> **Data Validation at Scale:** Pydantic automatically validates large-scale input data to ensure it matches the expected structure, reducing errors.
>
> **Error Handling and Debugging:** It provides clear error messages for invalid data, making production debugging faster and easier.
>
> **Automatic Parsing and Serialization:** Pydantic converts raw data into usable formats automatically, simplifying data processing.
>
> **Integrates Well with FastAPI:** It seamlessly works with FastAPI to define input/output models for handling HTTP data in production.
>
> **Ensures Robustness for Microservices:** Pydantic ensures consistent and valid data exchange between microservices to avoid integration issues.
>
> **Prevents Security Issues:** It validates data at entry points to block malicious inputs and enhance app security.

Now, as we understand the importance of Pydantic, lets move back to PydanticAI

## Key Features of PydanticAI:

> **Structured Response Handling** : Utilizes Pydantic to validate both static and streamed responses, ensuring reliable data processing.
>
> **Versatile Model Support** : Compatible with OpenAI, Gemini, and Groq models, with a straightforward interface for integrating additional ones.
>
> **Rooted in Pydantic Expertise** : Developed by the creators of Pydantic, the backbone of popular frameworks like LangChain, OpenAI SDK, and more.
>
> **Streamlined Dependency Management** : Introduces a type-safe dependency injection system to simplify testing and iterative development.
>
> **Pythonic Design** : Employs standard Python programming practices for agent composition and control flow, making it intuitive for developers.
>
> **Monitoring with Logfire** : Includes integration with Logfire for performance tracking and debugging of AI-driven applications.
>
> **Type-Safe Operations** : Ensures robust, type-checked workflows, minimizing runtime errors.
>
> **In Active Beta** : Currently in beta, offering room for ongoing enhancements and feedback-driven updates.

## Codes and Setup

Let’s quickly build a dummy PydanticAI Agent and see how it works

1. Pip install

```python pip install 'pydantic-ai-slim[openai,vertexai,logfire]' ```

2\. Set up the API token for your LLM provider. Pydantic Can work with OpenAI, Groq, and VertexAI straightaway.

2\. Create a PydanticAI Agent. This code implements a **bank support agent** using PydanticAI. It leverages structured dependencies, a defined result schema, and tools for interacting with customer data in a database.

```python from dataclasses import dataclassfrom pydantic import BaseModel, Fieldfrom pydantic_ai import Agent, RunContextfrom bank_database import DatabaseConn@dataclassclass SupportDependencies: customer_id: int db: DatabaseConn class SupportResult(BaseModel): support_advice: str = Field(description='Advice returned to the customer') block_card: bool = Field(description="Whether to block the customer's card") risk: int = Field(description='Risk level of query', ge=0, le=10)support_agent = Agent( 'openai:gpt-4o', deps_type=SupportDependencies, result_type=SupportResult, system_prompt=( 'You are a support agent in our bank, give the ' 'customer support and judge the risk level of their query.' ),)@support_agent.system_prompt async def add_customer_name(ctx: RunContext[SupportDependencies]) -> str: customer_name = await ctx.deps.db.customer_name(id=ctx.deps.customer_id) return f"The customer's name is {customer_name!r}"@support_agent.tool async def customer_balance( ctx: RunContext[SupportDependencies], include_pending: bool) -> float: """Returns the customer's current account balance.""" return await ctx.deps.db.customer_balance( id=ctx.deps.customer_id, include_pending=include_pending, )async def main(): deps = SupportDependencies(customer_id=123, db=DatabaseConn()) result = await support_agent.run('What is my balance?', deps=deps) print(result.data) """ support_advice='Hello John, your current account balance, including pending transactions, is $123.45.' block_card=False risk=1 """ result = await support_agent.run('I just lost my card!', deps=deps) print(result.data) """ support_advice="I'm sorry to hear that, John. We are temporarily blocking your card to prevent unauthorized transactions." block_card=True risk=8 """ ```

Understanding the codes

### Dataclass decorator (support dependencies)

* **Purpose** : Defines dependencies the agent needs, like the customer ID and a database connection (`db`).
* **Use** : Passed to the agent to access customer-specific data during queries

### SupportResult

* **Purpose** : Specifies the structured format for the agent’s output.

> `support_advice`: Textual advice for the customer.
>
> `block_card`: Whether the customer's card should be blocked.
>
> `risk`: Risk level (0-10) of the situation described in the query.

* **Validation** : Enforces data integrity (e.g., `risk` must be between 0 and 10).

### Agent Setup

* Uses the `openai:gpt-4o` model for generating responses.
* Specifies dependencies (`deps_type`) and expected output (`result_type`).
* Includes a **system prompt** that sets the context for the agent, instructing it to provide support and assess query risk.

### System prompt function

**Purpose** : Dynamically enriches the system prompt with the customer’s name by querying the database.

**How it Works** :

> Accesses the database (`ctx.deps.db`) using the customer ID (`ctx.deps.customer_id`).
>
> Returns a string with the customer’s name to personalize the interaction.

### Custom Tool

**Purpose** : Adds a tool to fetch the customer’s account balance, optionally including pending transactions.

**How it Works** :

> Queries the database using the customer ID and `include_pending` flag.
>
> Returns the balance as a float.

### Run the agent

**Purpose** : Demonstrates how to interact with the agent by passing queries and dependencies.

**Steps** :

> Create a `SupportDependencies` object with the customer ID and database connection.
>
> Call the agent’s `run` method with a query (`'What is my balance?'`).
>
> The agent generates a response based on the query, dependencies, and tools.

As you might have observed, the output is in the defined format as mentioned in the SupportResult (Risk is int, Block_card is Bool)class and hence, solves a big problem of structured output from LLMs.

## Concluding

In conclusion, PydanticAI offers a powerful framework for building robust, production-grade applications with generative AI. By leveraging Pydantic’s data validation capabilities and integrating seamlessly with LLMs, it ensures structured, type-safe, and secure interactions. Its ease of use, along with support for multiple AI models, makes it an invaluable tool for developers building AI agents. As PydanticAI continues to evolve, it promises to simplify the development of AI-driven applications and enhance their reliability and scalability.
