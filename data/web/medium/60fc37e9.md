---
domain: medium.com
fetch_date: '2026-05-18T12:48:38.713397'
status: ok
url: https://medium.com/@techsachin/training-llms-to-interact-and-infer-user-profiles-and-personalities-for-alignment-with-individual-e41ac869a05e
---

# Training LLMs to Interact and Infer user profiles and personalities for Alignment with individual preferences

[ ![SACHIN KUMAR](https://miro.medium.com/v2/resize:fill:64:64/1*7GE5_sjWH8e95wFj2s9sgg.jpeg) ](</@techsachin?source=post_page---byline--e41ac869a05e--------------------------------------->)

[SACHIN KUMAR](</@techsachin?source=post_page---byline--e41ac869a05e--------------------------------------->)

9 min read

·

Oct 7, 2024

\--

\--

Listen

Share

More

Previous research focused on general alignment to principles such as helpfulness, harmlessness, and honesty, the need to account for individual and diverse preferences has been largely overlooked, potentially undermining customized human experiences.

To address this gap, this paper[1] train LLMs that can “interact to align”, essentially cultivating the meta-skill of LLMs to implicitly infer the unspoken personalized preferences of the current user through multi-turn conversations, and then dynamically align their following behaviors and responses to these inferred preferences. It also establish the ALOE (ALign with custOmized prEferences) benchmark, consisting of 100 carefully selected examples and well designed metrics to measure the customized alignment performance during conversations.

Using this approach LLMs can implicitly infer user profiles and personalities, allowing them to progressively tailor responses to align with individual preferences.

![image](https://miro.medium.com/v2/resize:fit:700/1*bka2MMJBECAJ35dddtO01Q.png)

Key contributions:

* identify the limitations of the current alignment paradigm and emphasize the need for LLMs to dynamically adapt to individual preferences through interaction.
* propose a scalable data construction approach combining iterative self-generation and multi-LLM collaboration, establish a diverse and distinct persona pool, and construct a tree-structured multi-turn preference dataset.
* build a benchmark with 100 carefully curated examples and metrics to evaluate the LLMs’ capabilities of aligning to individual preferences dynamically.
* Experimental results showcase the deficiency of mainstream LLMs and prove the effectiveness of our approach.

## Approach

### i) Preference Data Construction

* To fine-tune LLMs and enhance their ability to dynamically align with individual preferences, authors create a pool of 3,310 distinct personas.
* These personas then guide conversations, resulting in a multi-turn preference dataset comprising over 3K multi-turn conversations.

**a) Persona Pool**

* adopt an iterative self-generation and filtering pipeline for establishing the profile and personality pools, as shown in figure below

![image](https://miro.medium.com/v2/resize:fit:529/1*7t9qAOTGfYJLKIKMD7m9uw.png)
* manually craft 20 seed user profiles to establish a base pool and initiate iteration.
* In each round, 5 profiles are randomly selected from the current pool as few-shot examples and used as input to an off-the-shelf LLM (GPT-4o), which generates a batch of 20 new user profiles per iteration.
* Then we introduce an automatic filtering process based on semantic similarity to ensure the distinctiveness and diversity of the pool.
* adopt the Sentence Transformers to compute the embedding of profile descriptions and measure the cosine similarity between new and existing profiles.
* If the highest similarity score exceeds 0.6,the new profile is considered too similar and discarded. Otherwise, it will be added to the profile pool.
* We repeat the process until a bottleneck is reached, where few new profiles can be added to the pool
* personality pool is also built using this pipeline.
* The final profile pool consists of 330 instances, and the personality pool consists of 71 instances. we randomly selected profiles and personalities from each pool, creating a total of 3,310 comprehensive, distinct, and diverse user personas

**b) Preference Dataset**

this approach is based on multi-LLM collaboration to manually break down the complex task into manageable subtasks, as shown in figure below:

![image](https://miro.medium.com/v2/resize:fit:700/1*wHcDZywQ7fTShCrIc1TBpg.png)

* For each persona in the pool, we task a roleplaying LLM to take on the role of that user and engage in conversation with chatting LLMs.
* The role-playing LLM is instructed to select appropriate, personalized topics based on the user profile description and adapts its communication style to reveal the user’s personality.
* For each conversation turn i, the role-playing LLM generates a simulated user’s message mi. To generate pairwise responses for each round, we consider two different lines

(1) First, we instruct an induction LLM to analyze the previous conversations and extract the persona that has been revealed from the complete user persona description. The extracted persona, together with the user’s message, is then provided to the preferred LLM to generate a tailored response for this specific user, which we label as the “preferred” response pi.

(2) Second, we instruct a rejected LLM to directly generate the response given only the user’s message without persona information, which we label as the “rejected” response ri. One of these responses is randomly selected (denoted as si) for the role-playing LLM to continue the conversation

* In this implementation, authors create up to 10 rounds of conversations across the entire persona pool and create a tree-structured multi-turn preference dataset consisting of 3K+ training examples. Each example is denoted as:

![image](https://miro.medium.com/v2/resize:fit:177/1*XsdHcJSFLr9CBV8N05YLMA.png)

### ii) Training

**a) Supervised Fine-tuning**

* In this stage, we train LLMs only on the preferred response with the training objective:

![image](https://miro.medium.com/v2/resize:fit:446/1*uPXsjn4LJ-9-LN-nUGersw.png)

where P(pi|mi, {mj , sj}i−1j=1; θ) is the conditional probability of the model (parameterized by θ) generating the preferred response pi given the message mi, the conversations {mj , sj}i−1j=1 before i turn. K is the total number of interaction turns

**b) Reinforcement Learning**

* perform reinforcement learning (RL) using the Direct Preference Optimization (DPO) algorithm with the pairwise preference dataset we construct:

![image](https://miro.medium.com/v2/resize:fit:418/1*NUqb8j4g2urcOp4fjAQhGw.png)

where Pθ(pi|mi, si) is the probability of the model (parameterized by θ) generating the preferred response pi given the message mi and state si, Pθ(ri|mi, si) is the probability of the model generating the rejected response ri, θ′ denotes the reference model, σ(·) is the sigmoid function, and β is a parameter controlling the deviation from the reference model. The state si refers to the conversations {mj , sj}i−1j=1 before i turn.

**c) Implementation**

* apply a linear learning rate scheduler with a learning rate of 1e-5,a batch size of 48, and 3 training epochs.
* Similarly, for DPO, we use a linear learning rate scheduler with a learning rate of 1e-5, β set to 0.9, a batch size of 48, and 1 training epoch.

## ALOE Benchmark

* developed a benchmark consisting of 100 carefully curated instances and well-designed metrics for evaluation

### i) Benchmark Construction

* adopt the same procedure discussed in Section above to create the evaluation benchmark, but with careful human verification.
* each of the 100 evaluation cases contains a distinct user persona, including the profile and personality descriptions.
* ask human annotators to verify each test case to ensure that the selected personas are the most distinct, diverse, and sufficiently different from those used in training.

### ii) Evaluation Protocol

* adopt an off-the-shelf LLM (GPT-4o) to serve as the role-playing LLM to mimic each provided persona and engage in 10 rounds of conversation with the evaluated models

### iii) Metrics

* adopt the LLM-as-a-Judge approach to evaluate the quality of responses.
* For each conversation round, we prompt GPT-4o with the full user persona, the user’s message, and the evaluated model’s response, asking it to rate how well the response aligns with the user’s potential preferences on a scale from 1 to 5.
* Then we calculate an average score among 100 evaluation cases for each round of conversation, which we define as our primary metric, Alignment Level (k-Turn), abbreviated as AL(k).
* introduce a metric called the Improvement Rate (IR), to evaluate the model’s progressive alignment with user preferences throughout the conversation. It is is computed as the regression coefficient b from the least-square regression

![image](https://miro.medium.com/v2/resize:fit:353/1*eCzS-QBtugLIwWVQ_C1Pgw.png)

where k denotes the k-th conversation turn. Recognizing that the relationship between AL(k) and k may not be strictly linear

* also measure and report the normalized IR (N-IR) to account for the influence of higher initial alignment levels, which can limit potential improvement and result in a smaller absolute slope of the estimated curve. Specifically, they normalize AL(k) by applying the following formula before performing the least-square regression:

![image](https://miro.medium.com/v2/resize:fit:421/1*M1T3FP-da7a-uky1y3hrBw.png)

## Results

* Table below shows experimental results of mainstream open-source LLMs trained with different strategies. The underlined types are for ablation study. We use pink to indicate the highest average AL (Alignment Level), teal for the highest IR (Improvement Rate) and N-IR, and yellow for the second highest IR and N-IR

![image](https://miro.medium.com/v2/resize:fit:700/1*mraze8blvmHyOVTsQz4VFw.png)
* Among the evaluated open-source LLMs (Base), Qwen2 outperforms others, achieving the highest average AL of 4.67 across all conversation turns, along with the highest IR, with a regression coefficient (slope) of 0.254 and an R2 of 0.917. It shows that Qwen model not only generates superior responses in each turn but also effectively infers and extracts user information and preferences over the course of conversations, highlighting its strong capacity for alignment through interaction.
* In contrast, the other three LLMs demonstrate an average AL below 3 and IR under 0.01, underscoring a notable gap in current LLMs’ capacity to dynamically adjust to individual preferences
* observe that all four models exhibit significant improvements in both average AL and IR compared to the Base LLMs. An exception is Qwen2, where our approach shows a slight lag compared to the Base LLM in terms of IR
* Figure below shows Visualized performance of four base LLMs and their fine-tuned variants across ten conversation rounds.

![image](https://miro.medium.com/v2/resize:fit:700/1*0ir6uxLgYyPSWW2VTbZ_VQ.png)
* LLMs tuned using this approach generally increase the AL for each turn and reach a larger improvement rate, evidenced by a steeper positive linear regression fitting line in a much higher position.

## Analysis

### i) Effectiveness of Using Pairwise Responses via Reinforcement Learning

* Results in tabe above, shows that average relative improvement on average AL is10.0%. Especially, for LLMs with relatively lower performance using only SFT (e.g., OLMo), incorporating RL yields the highest average performance improvement in AL (26.02%)
* It demonstrates that our two-stage training framework, combined with tree-structured preference dataset construction, effectively addresses the final performance gap and is widely effective

### ii) Our Approach Generates High-Quality Pairwise Responses

* Results show that performance consistently diverged across all models, with a relative difference exceeding 10.97%, consistently favoring SFT-Preferred.
* This demonstrates that the preferred responses are significantly better, offering distinct pairs suitable for high-quality RL training

### iii) Influence of Agent Data

* implemented SFT on Qwen2 utilizing three types of data mixtures: (1) Preferred responses in ALOE and CodeActInstruct (Mixture), (2) CodeActInstruct only (CodeActInstruct), and (3) Preferred responses in ALOE only (Preferred).
* Table below shows that the agent data — consisting of multi-turn interactions with the environment — contributes to the interaction capabilities of LLMs, achieving the highest IR.

![image](https://miro.medium.com/v2/resize:fit:700/1*qSzwklshBzmLdXXhjHBoTA.png)
* preferred responses in ALOE optimize LLMs for conversational domains, enhancing their fundamental capabilities to alignment via interaction in this context, evidenced by the significant improvement on average AL.

### iv) Human Annotation

* uniformly sample 5 responses from each of the 100 evaluation cases per conversation turn, yielding 50 samples per annotator.
* Three human annotators are then instructed to rate each response on a scale of 1 to 5, producing three sets of human-rated AL across 10 conversation turns.
* Next, we calculate the Cohen’s Kappa coefficient between each human ratings set and the GPT-4o ratings set.
* The average Cohen’s Kappa reaches 0.789, indicating strong agreement between human and LLM judges and validating the reliability of our auto-evaluation method

## Limitations

* In this implementation of training and evaluation, authors limit interactive turns to 10 due to resource constraints in training long-context LLMs.
* This constraint may limit the model’s ability to engage in complex, nuanced conversations that require extended dialogue, potentially affecting how well it understands and aligns with the user’s persona.
* It may also mask the model’s shortcomings in aligning with individual preferences during deeper interactions.

## Conclusion

* introduces a novel approach to aligning LLMs’ behaviors with individual user preferences by training models to “interact to align”.
* evaluation using ALOE demonstrates the success of our approach in improving personalized alignment performance

Paper: <https://arxiv.org/abs/2410.03642>

Code: <https://github.com/ShujinWu-0814/ALOE>

References:

1. Aligning LLMs with Individual Preferences via Interaction by Wu et al. [arXiv:2410.03642](<https://arxiv.org/abs/2410.03642>)
