---
domain: medium.com
fetch_date: '2026-05-18T12:47:19.696509'
status: ok
url: https://medium.com/@bnjmn_marie/estimate-the-memory-consumption-of-llms-for-inference-and-fine-tuning-c28c8b9e3274
---

# Estimate the Memory Consumption of LLMs for Inference and Fine-tuning

## A close look at the memory consumption of Command-R+, Mixtral-8x22B, and Llama 3 70B

[ ![Benjamin Marie](https://miro.medium.com/v2/resize:fill:64:64/1*sifLT7ybERpQ7SnaPwBDBQ.png) ](</@bnjmn_marie?source=post_page---byline--c28c8b9e3274--------------------------------------->)

[Benjamin Marie](</@bnjmn_marie?source=post_page---byline--c28c8b9e3274--------------------------------------->)

9 min read

·

Apr 27, 2024

\--

\--

Listen

Share

More

![Generated with DALL-E](https://miro.medium.com/v2/resize:fit:622/0*QK8ZrPWd4nj8Gw1n.png)

With Command-R+, Mixtral-8x22b, and Llama 3 70B that were all released within a few weeks, we have now LLMs that perform more and more closely to the best GPT-4 models. However, these models are huge. They all have more than 70 billion parameters:

* [Command-R+](<https://huggingface.co/CohereForAI/c4ai-command-r-plus>): A 104B parameter model
* [Mixtral-8x22b](<https://huggingface.co/mistralai/Mixtral-8x22B-v0.1>): A mixture-of-expert (MoE) model with 141B parameters
* [Llama 3 70B](<https://huggingface.co/meta-llama/Meta-Llama-3-70B>): A model with 70.6B parameters

_Can you fine-tune and run these models on your computer?_

In this article, I explain and analyze their memory consumption for inference and fine-tuning. The method I present applies to any transformer LLMs to estimate their memory consumption without downloading them. We will see that, while the memory consumption of Command-R+, Mixtral-8x22b, and Llama 3 70B is huge, there are several techniques to significantly reduce it, such as quantization and memory-efficient optimizers.

I made a notebook that can automatically estimate the memory consumption of a transformer model for inference and fine-tuning. You can find it here:

[Get the notebook (#64)](<https://kaitchup.substack.com/p/notebooks>)

## Memory Required for Inference with Command-R+, Mixtral-8x22B, and Llama 3 70B

### Loading the model

These three models are all distributed with 16-bit weights: float16 for Command-R+ and bfloat16 for Mixtral and Llama 3 70B. In other words, one parameter consumes 16 bits, or 2 bytes, of memory.

1 billion parameters occupy 2 billion bytes. 1 billion bytes is 1 gigabyte, meaning that 1B parameters occupy 2 GB of memory. 100B parameters occupy 200 GB of memory. _Note: This is an approximation since 1 KB is not equal to 1,000 bytes but to 1,024 bytes._

To know how many parameters a model has without downloading it, you can check the model card:

![Screenshot by the author of this page](https://miro.medium.com/v2/resize:fit:700/0*I6hPHBmSv_Kx0_fp.png)

If you want to do fast inference on GPUs, you will need to load the model entirely on the GPU RAM. It means that you will need:

* For Command-R+: 193.72 GB of GPU RAM
* For Mixtral-8x22B: 262.63 GB of GPU RAM
* For Llama 3 70B: 131.5 GB of GPU RAM

In other words, you will need 2x80 GB GPUs, e.g., two H100s, to load Llama 3 70B, one more GPU for Command-R+, and another one for Mixtral.

### Memory Consumption of Activations

Once the model is loaded, we need more memory to store the model’s activations, i.e., the tensors created during inference. These tensors are passed from one layer to the next one. Their size in memory is not straightforward to estimate.

To estimate the memory consumption of these activations, I use the method proposed in this paper:

[Reducing Activation Recomputation in Large Transformer Models](<https://arxiv.org/pdf/2205.05198.pdf>)

We need to know the following to estimate the memory consumption:

* s: The maximum sequence length (the number of tokens in the input)
* b: The batch size
* h: The hidden dimensions of the model
* a: The number of attention heads

A standard transformer layer is composed of a self-attention block and an MLP block, each connected by two layer-norms. The memory consumption for each component is estimated as follows.

**For the self-attention**

The attention block consists of self-attention mechanisms and a linear projection. The memory requirements include:

_Note: When I write “sbh”, read it as a multiplication “s*b*h”._

* The linear projection retains its input activations, which have a size of 2sbh.
* The linear projection and self-attention input activations, requiring 2sbh each.
* Self-attention’s Query (Q) and Key (K) matrices needing 4sbh combined.
* Softmax and its dropout requiring 2as²b and as²b respectively.
* Storage for the attention applied over Self-attention’s Values (V) adding up to 2as²b + 2sbh.

The total memory required for the attention block amounts to 10sbh + 5as²b.

**For the MLP block**

The MLP block involves two linear layers:

* The linear layers store inputs which cost 2sbh and 8sbh, with the GeLU non-linearity also needing 8sbh. _Note: We probably don’t need to store GeLU’s activations for inference but I’ll still count them to give an estimation of the memory consumption at worst, in case the framework used for inference stores these activations._

Overall, the MLP block demands 18sbh of storage.

**For the layer-norms**

Each layer-norm stores its input, needing 2sbh each, summing to 4sbh for the two norms.

**For an entire layer**

The total memory requirement to store activations for one transformer layer totals 10sbh + 5as²b (from the attention block) + 18sbh (from the MLP block) + 4sbh (from the layer norms).

**Activation memory consumption per layer = 32 sbh + 5as²b**

If we use a 16-bit data type, which is common for activations, we have to multiply this number by 2 since each activation parameter would cost 2 bytes.

This equation roughly approximates the real memory consumption of the activations. Most inference frameworks are optimized to significantly reduce it by removing tensors as soon as they are useless rather than keeping them until the layer is fully processed. On the other hand, various buffers consuming memory are also created during inference. Nonetheless, in my experiments, I found it yields a good approximation with frameworks such as Hugging Face Transformers. With other frameworks that are much more optimized, such as vLLM and TGI, the memory consumption is greatly reduced.

## [vLLM: Serve Fast Mistral 7B and Llama 2 Models from Your ComputerOffline inference and serving with quantized modelskaitchup.substack.com](<https://kaitchup.substack.com/p/vllm-serve-fast-mistral-7b-and-llama?source=post_page-----c28c8b9e3274--------------------------------------->)

If you use advanced techniques such as FlashAttention, Alibi, or RoPE, the memory consumption for processing long sequences will also be greatly reduced.

## [Use FlashAttention-2 for Faster Fine-tuning and InferenceHow to use FlashAttention-2 for QLoRA fine-tuningkaitchup.substack.com](<https://kaitchup.substack.com/p/use-flashattention-2-for-faster-fine?source=post_page-----c28c8b9e3274--------------------------------------->)

### Estimating the Memory Consumption for Command-R+, Mixtral-8x22B, and Llama 3 70B for Inference

I used the notebook to automatically estimate the memory consumption of these models without downloading them.

[Get the notebook (#63)](<https://kaitchup.substack.com/p/notebooks>)

We need to estimate the size of the model and add the size of the activations for one layer.

The estimations were computed for the standard scenario in which both the models’ parameters and activations are 16-bit.

We also need to set the decoding hyperparameters. I used:

* s = 512 (the sequence length)
* b = 8 (the batch size)

It yields:

![Table by the author](https://miro.medium.com/v2/resize:fit:646/0*fanfyrWgBHTQikQg.png)

Compared to the size of the model in memory, the size of the activations is negligible. However, their size rapidly increases as the batch size and the sequence length get larger.

### Reducing the Memory Consumption for Inference

Most of the memory consumption for inference comes from the model’s parameters. Recent quantization algorithms can significantly reduce this memory consumption. They compress the model by reducing the bit width of most parameters while trying to preserve its accuracy.

8-bit quantization is almost lossless while 4-bit quantization only slightly degrades the performance.

## [From 16-bit to 2-bit: Finding the Best Trade-off Between Memory-Efficiency and AccuracyMistral 7B and Llama 2 under pressurekaitchup.substack.com](<https://kaitchup.substack.com/p/from-16-bit-to-2-bit-finding-the?source=post_page-----c28c8b9e3274--------------------------------------->)

4-bit quantization divides by 4 the memory consumption of the model since most parameters are 4-bit, i.e., 0.5 bytes instead of 2 bytes. I recommend AWQ for 4-bit quantization which is simple to run and yields fast models.

## [Fast and Small Llama 3 with Activation-Aware Quantization (AWQ)Better, fast, and more simple than GPTQ quantizationkaitchup.substack.com](<https://kaitchup.substack.com/p/fast-and-small-llama-2-with-activation?source=post_page-----c28c8b9e3274--------------------------------------->)

However, for very large models with more than 100B parameters, quantization with a lower precision, e.g., 2.5 or 3-bit, can still yield accurate models. [AQLM for instance shows good performance for 2-bit quantization applied to Mixtral-8x7B](<https://kaitchup.substack.com/p/run-a-77x-smaller-mixtral-8x7b-on>). The problem with AQLM is that quantizing the model is very costly. It can take several weeks in the cloud for very large models.

Another alternative is to move the model to another memory device, for instance, the CPU RAM. The downside is that it slows down inference, especially with batch decoding. Frameworks optimized for CPU inference help to keep inference reasonably fast. Neural Speed for instance is one of the fastest frameworks for inference on CPU with quantized models.

## [Neural Speed: Fast Inference on CPU for 4-bit Large Language ModelsUp to 40x faster than llama.cpp?kaitchup.substack.com](<https://kaitchup.substack.com/p/neural-speed-fast-inference-on-cpu?source=post_page-----c28c8b9e3274--------------------------------------->)

Nonetheless, even if you use the CPU, you’ll still need a lot of CPU RAM to load the model and store the activations.

## Memory Required for Fine-tuning Command-R+, Mixtral-8x22B, and Llama 3 70B

For fine-tuning LLMs, estimating the memory consumption is slightly more complicated. In addition to storing the model weights and activations, for all layers, we also need to store the optimizer states.

### Memory Consumption of the Optimizer States

The AdamW optimizer, which is the most popular for fine-tuning LLMs, creates and stores 2 new parameters for each parameter of the model. If we have a 100B model, the optimizer will create 200B new parameters!

Moreover, for better training stability, the optimizers’ parameters are float32, i.e., each parameter occupies 4 bytes of memory.

The optimizer alone is the main reason why fine-tuning consumes more memory than inference.

For instance, for Mixtral-8x22B, the optimizer creates 2*141B = 282B float32 parameters. It consumes 1,053.53 GB of memory to which we have to add the memory occupied by the model itself, i.e., 262.63 GB. In total, we need 1,315.63 GB of GPU memory. That’s roughly seventeen 80 GB GPUs!

And then, that’s not nearly enough to fine-tune the model. We also need memory to store the activations.

### Memory Required to Compute the Gradients

In contrast with inference for which we only need to store activations for a single layer before passing them to the next one, fine-tuning requires storing all the activations created during the forward pass. This is necessary to compute the gradients which are then used to backpropagate the errors and updates the model’s weights.

To estimate the memory necessary to compute the gradients, we can use the same formula used for inference and then multiply the result by the number of layers. We also need to take into account the dropout masks for the self-attention and MLP modules, which add 2 sbh.

If L is the number of layers, then the memory consumed to compute the gradients is

**L(34sbh + 5as²b)**

### Estimating the Memory Consumption of Llama 3 70B, Mixtral-8x22B, and Command R+ for Fine-tuning

The notebook can also estimate the memory required for fine-tuning without downloading the model:

[Get the notebook (#63)](<https://kaitchup.substack.com/p/notebooks>)

We need to estimate the size of the model and add the size of the activations for all layers and the size of the optimizer states.

I set the following hyperparameters for fine-tuning:

* s = 512 (the sequence length)
* b = 8 (the batch size)

For the optimizer states, I assume they are float32.

It yields:

![Table by the author](https://miro.medium.com/v2/resize:fit:644/0*QRhVN90UqF2B-XTv.png)

This is memory consumption at worst, i.e., without using any optimizations to reduce it. We are very far from an affordable hardware configuration. Fortunately, there are many optimizations that we can apply to reduce the memory requirements.

### Reducing the Memory Consumption for Fine-tuning

Since the optimizer states consume a huge amount of memory, significant research has been done to reduce their memory footprint, e.g.:

* LoRA: Freezes the entire model and adds a trainable adapter with a few million parameters. With LoRA, we only store the optimizer states for the adapter’s parameters.
* QLoRA: LoRA but with the model quantized to 4-bit or lower precision.

## [QLoRA: Fine-Tune a Large Language Model on Your GPUFine-tuning models with billions of parameters is now possible on consumer hardwarekaitchup.substack.com](<https://kaitchup.substack.com/p/qlora-fine-tune-a-large-language-model-on-your-gpu-27bed5a03e2b?source=post_page-----c28c8b9e3274--------------------------------------->)

* AdaFactor and AdamW-8bit: Much more memory-efficient optimizers that deliver a performance close to AdamW. AdaFactor can be unstable during training.
* GaLore: Project the gradients into low-rank subspaces which can reduce the size of the optimizer states by 80%.

## [GaLore: Full Fine-tuning on Your GPUAnd pre-training!kaitchup.substack.com](<https://kaitchup.substack.com/p/galore-full-fine-tuning-on-your-gpu?source=post_page-----c28c8b9e3274--------------------------------------->)

Another significant part of the memory is consumed by the activations. To reduce it, gradient checkpointing is usually employed. It recomputes some of the activations when needed to compute the gradients. It reduces memory consumption but also slows down fine-tuning.

Finally, there are frameworks such as Unsloth that are extremely optimized for fine-tuning with LoRA and QLoRA.

## [Fine-tune a Better Google Gemma with Unsloth and Distilled DPOThe Zephyr recipe on a budgetkaitchup.substack.com](<https://kaitchup.substack.com/p/fine-tune-a-better-google-gemma-with?source=post_page-----c28c8b9e3274--------------------------------------->)

## Conclusion

In this article, we saw how to estimate the memory consumption of transformer models. This estimation is not trivial and the method I described can only approximate it while disregarding any optimizations of the frameworks used.

This method doesn’t work for models using other architectures than the transformer. For instance, the memory consumption of the activations for Mamba and RWKV is significantly lower since they don’t use the attention mechanism of the transformer.

To support my work, consider subscribing to my newsletter for more articles/tutorials on recent advances in AI:

## [The Kaitchup - AI on a BudgetWeekly news, tips, and tutorials on fine-tuning, running, and serving large language models on your computer. Each…kaitchup.substack.com](<https://kaitchup.substack.com/?source=post_page-----c28c8b9e3274--------------------------------------->)
