---
domain: pub.towardsai.net
fetch_date: '2026-05-18T12:50:48.035090'
status: ok
url: https://pub.towardsai.net/fine-tuning-bert-for-phishing-url-detection-a-beginners-guide-619fad27db41
---

# Fine-Tuning BERT for Phishing URL Detection: A Beginner’s Guide

[ ![Anoop Maurya](https://miro.medium.com/v2/resize:fill:64:64/1*GpLTfdPuDfw-8acfG4GhZg.jpeg) ](<https://medium.com/@mauryaanoop3?source=post_page---byline--619fad27db41--------------------------------------->)

[Anoop Maurya](<https://medium.com/@mauryaanoop3?source=post_page---byline--619fad27db41--------------------------------------->)

7 min read

·

Oct 19, 2024

\--

Listen

Share

More

Press enter or click to view image in full size

Photo by [Amr Taha™](<https://unsplash.com/@amr_taha?utm_source=medium&utm_medium=referral>) on [Unsplash](<https://unsplash.com/?utm_source=medium&utm_medium=referral>)

### Stuck behind a paywall? [Read for Free](<https://medium.com/@mauryaanoop3/fine-tuning-bert-for-phishing-url-detection-a-beginners-guide-619fad27db41?sk=dbf8da6003aadee1d90e641bf0f938f2>)!

In the realm of artificial intelligence, the emergence of transformer models has revolutionized natural language processing (NLP). While large models with billions of parameters dominate the landscape, smaller models can still deliver impressive results. In this guide, we will explore how to fine-tune BERT, a model with 110 million parameters, specifically for the task of phishing URL detection. We will cover essential concepts and provide a comprehensive example using Python code.

## Understanding Phishing and Its Importance

Phishing is a form of cybercrime where attackers impersonate legitimate entities to deceive individuals into revealing sensitive information, such as usernames, passwords, or credit card details. Phishing URLs are often the first point of contact in these scams, making their detection crucial for cybersecurity.Detecting phishing URLs can significantly reduce the risk of falling victim to these attacks. Machine learning models, particularly those based on deep learning architectures like BERT, have shown great promise in identifying malicious URLs by analyzing their textual features.

## What is BERT?

BERT (Bidirectional Encoder Representations from Transformers) is a transformer-based model designed to understand the context of words in a sentence. Unlike traditional models that read text sequentially, BERT processes text bidirectionally, allowing it to capture nuanced meanings effectively. This capability makes it particularly suitable for tasks like text classification, where understanding context is key.

> Beginner-Friendly Explanation of the Code for Fine-Tuning BERT for Phishing URL Detection

In this section, we will break down the code step-by-step in a way that is easy to understand for beginners. Each part of the code serves a specific purpose in the process of fine-tuning BERT to detect phishing URLs. Let’s dive in!

## If you like the article and want to show some love:

* **Clap** 50 times — each one helps more than you think! 👏
* **Follow** me here on Medium and subscribe for free to catch my latest posts. 🫶
* Let’s connect on **LinkedIn** , check out my projects on [**GitHub**](<https://github.com/imanoop7>), and stay in touch on **Twitter**!
* If you found this project useful, don’t forget to ⭐ the repo on [**GitHub**](<https://github.com/imanoop7/Fine-Tuning-BERT-Model-for-Text-Classification>). It helps others find it too!

## [Fine-Tuning Phi-3 with Unsloth for Superior Performance on Custom Data:pub.towardsai.net](</fine-tuning-phi-3-with-unsloth-for-superior-performance-on-custom-data-2c14b3c1e90b?source=post_page-----619fad27db41--------------------------------------->)

## Setting Up Your Environment

Before we dive into the code, ensure you have the necessary libraries installed. You can set up your environment using pip:

```python pip install transformers datasets evaluate ```

## Loading and Preparing the Dataset

For our phishing URL detection task, we will use a dataset available on Hugging Face. The dataset contains labeled URLs indicating whether they are safe or not.Here’s how to load and prepare the dataset:

```python from datasets import load_datasetprint("Starting script...")# Load the dataset from Hugging Faceprint("Loading dataset...")dataset = load_dataset("imanoop7/phishing_url_classification")print(f"Dataset loaded. Keys: {dataset.keys()}") ```

**Explanation:**

* We import the `load_dataset` function from the `datasets` library to fetch our dataset.
* The dataset is loaded from Hugging Face, specifically designed for phishing URL classification. The keys of the dataset are printed to confirm successful loading.

## Creating Train/Validation/Test Splits

```json # Check available splits and create train/validation/test splits if necessaryif "train" not in dataset or "validation" not in dataset or "test" not in dataset: print("Creating train/validation/test splits...") combined_data = dataset["train"] if "train" in dataset else dataset[list(dataset.keys())[0]] # Create train/validation/test splits splits = combined_data.train_test_split(test_size=0.2, seed=42) train_valid = splits["train"] test = splits["test"] splits = train_valid.train_test_split(test_size=0.1, seed=42) train = splits["train"] validation = splits["test"] dataset_dict = { "train": train, "validation": validation, "test": test }else: print("Using existing dataset splits...") dataset_dict = datasetprint(f"Dataset splits: {dataset_dict.keys()}") ```

**Explanation:**

* This block checks if the dataset already has predefined splits for training, validation, and testing.
* If not, it creates these splits by first dividing the training data into a training set and a test set (80/20 split). Then, it further divides the training set into training and validation sets (90/10 split).
* The resulting datasets are stored in a dictionary for easy access.

## [A Beginner’s Guide to Fine-Tuning an Embedding Model:Stuck behind a paywall? Read for Free!generativeai.pub](<https://generativeai.pub/a-beginners-guide-to-fine-tuning-an-embedding-model-38bb4b4ae664?source=post_page-----619fad27db41--------------------------------------->)

## Tokenizing the Data

Next, we need to tokenize our data using BERT’s tokenizer. This step converts our text data into a format that BERT can understand.

```python from transformers import AutoTokenizer# Define pre-trained model pathmodel_path = "bert-base-uncased"# Load model tokenizerprint("Loading tokenizer...")tokenizer = AutoTokenizer.from_pretrained(model_path)# Define text preprocessing functiondef preprocess_function(examples): return tokenizer(examples["text"], truncation=True)# Preprocess all datasetsprint("Preprocessing datasets...")tokenized_data = {}for split, dataset in dataset_dict.items(): print(f"Preprocessing {split} split...") tokenized_data[split] = dataset.map(preprocess_function, batched=True) print(f"{split} split preprocessed. Size: {len(tokenized_data[split])}") ```

Press enter or click to view image in full size

image by author

**Explanation:**

* We import `AutoTokenizer` to load the tokenizer for BERT.
* The tokenizer is initialized using the pre-trained BERT model (`bert-base-uncased`).
* A preprocessing function is defined to tokenize the input text while truncating it to fit BERT’s input size.
* The datasets are then tokenized using this function, and the processed data is stored in a new dictionary.

## [Fine-tuning LLMs for Natural Language to SQL Query Generation Using Synthetic Data: A…Stuck behind a paywall? Read for Free!pub.towardsai.net](</fine-tuning-llms-for-natural-language-to-sql-query-generation-using-synthetic-data-a-comprehensive-38afdafc90b0?source=post_page-----619fad27db41--------------------------------------->)

## Setting Up the Model

We will now load BERT with a binary classification head to distinguish between safe and unsafe URLs.

```python from transformers import AutoModelForSequenceClassification# Load model with binary classification headprint("Loading model...")id2label = {0: "Safe", 1: "Not Safe"}label2id = {"Safe": 0, "Not Safe": 1}model = AutoModelForSequenceClassification.from_pretrained( model_path, num_labels=2, id2label=id2label, label2id=label2id,) ```

Press enter or click to view image in full size

image by author

**Explanation:**

* We import `AutoModelForSequenceClassification` to load a pre-trained BERT model adapted for sequence classification tasks.
* The model is set up with two labels: “Safe” and “Not Safe,” which correspond to phishing detection.
* The mapping between label IDs and their corresponding names is established.

## [Fine-tuning Microsoft PHI3 with Unsloth for Mental Health Chatbot DevelopmentPHI3 + Unsloth: The secret sauce for chatbots that understand your mental well-being.medium.com](<https://medium.com/@mauryaanoop3/fine-tuning-microsoft-phi3-with-unsloth-for-mental-health-chatbot-development-ddea4e0c46e7?source=post_page-----619fad27db41--------------------------------------->)

## Freezing Model Parameters

To fine-tune the model effectively while preserving its learned features, we will freeze most parameters except for those in the pooling layer.

``` print("Freezing base model parameters...")for name, param in model.base_model.named_parameters(): param.requires_grad = False if "pooler" in name: param.requires_grad = True ```

**Explanation:**

* This block freezes most parameters of the base BERT model to retain its learned features while allowing only the pooling layer’s parameters to be updated during training.
* By doing this, we reduce the risk of overfitting on our smaller phishing URL dataset while still enabling fine-tuning.

## Setting Up Training Arguments

```python from transformers import TrainingArguments, Trainerimport evaluateimport numpy as np# Load metricsprint("Loading metrics...")accuracy = evaluate.load("accuracy")auc_score = evaluate.load("roc_auc")def compute_metrics(eval_pred): predictions, labels = eval_pred probabilities = np.exp(predictions) / np.exp(predictions).sum(-1, keepdims=True) positive_class_probs = probabilities[:, 1] auc = np.round(auc_score.compute(prediction_scores=positive_class_probs, references=labels)['roc_auc'], 3) predicted_classes = np.argmax(predictions, axis=1) acc = np.round(accuracy.compute(predictions=predicted_classes, references=labels)['accuracy'], 3) return {"Accuracy": acc, "AUC": auc}# Define training argumentsprint("Setting up training arguments...")training_args = TrainingArguments( output_dir="bert-phishing-classifier", learning_rate=2e-5, per_device_train_batch_size=16, per_device_eval_batch_size=16, num_train_epochs=5, weight_decay=0.01, evaluation_strategy="epoch", save_strategy="epoch", load_best_model_at_end=True,) ```

**Explanation:**

* We import necessary components for training and evaluation.
* Metrics such as accuracy and AUC (Area Under Curve) are loaded for performance evaluation.
* A function `compute_metrics` is defined to calculate these metrics during evaluation.
* Training arguments are specified, including learning rate, batch sizes, number of epochs, weight decay for regularization, and strategies for evaluation and saving models.

## [Bridging the Gap: Fine-Tuning Phi-3 for SQL Query Generation with Natural Language Queries:pub.towardsai.net](</bridging-the-gap-fine-tuning-phi-3-for-sql-query-generation-with-natural-language-queries-8fbe6a58b1ec?source=post_page-----619fad27db41--------------------------------------->)

## Training the Model

``` # Initialize trainerprint("Initializing trainer...")trainer = Trainer( model=model, args=training_args, train_dataset=tokenized_data["train"], eval_dataset=tokenized_data["validation"], tokenizer=tokenizer,)# Train the modelprint("Starting training...")trainer.train()print("Training completed.") ```

**Explanation:**

* The `Trainer` class is initialized with our model, training arguments, tokenized datasets for training and validation, and tokenizer.
* The `trainer.train()` method is called to start the fine-tuning process on our phishing URL detection task.

## Evaluating Model Performance

``` # Evaluate on test datasetprint("Evaluating on test dataset...")test_results = trainer.evaluate(tokenized_data["test"])print("Test results:")print(test_results)print("Script completed successfully.") ```

Press enter or click to view image in full size

image by author

**Explanation:**

* After training completes, we evaluate the model on a separate test set to assess its performance on unseen data.
* The evaluation results are printed out to provide insights into how well the model can detect phishing URLs.

## Conclusion

Fine-tuning BERT for phishing URL detection is an effective way to leverage powerful NLP techniques for cybersecurity applications. By following this guide and utilizing the provided code snippets, beginners can successfully implement their own phishing URL classifier using BERT. As you gain more experience with fine-tuning models and working with different datasets, you can explore additional techniques such as hyperparameter tuning and data augmentation to further enhance your model’s performance.

## Additional Resource:

**Complete Code:**<https://github.com/imanoop7/Fine-Tuning-BERT-Model-for-Text-Classification>
**Fine Tuned Model:** <https://huggingface.co/imanoop7/bert-phishing-detector>
**Dataset:**[https://github.com/ollama/ollama?tab=readme-ov-file](<https://huggingface.co/datasets/imanoop7/phishing_url_classification>)
**My GitHub:**<https://github.com/imanoop7>
**LinkedIn:** [www.linkedin.com/in/anoop-maurya-908499148](<http://www.linkedin.com/in/anoop-maurya-908499148>)
**X:**<https://x.com/imanoop_7>
