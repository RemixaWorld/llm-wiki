---
domain: medium.com
fetch_date: '2026-05-18T12:47:48.089092'
status: ok
url: https://medium.com/neuml/introducing-rag-with-txtai-f3456977cf91
---

# Introducing RAG with txtai

## Get up and running fast with this easy-to-use application

[ ![David Mezzetti](https://miro.medium.com/v2/resize:fill:64:64/2*3fwd97jd7MTO4d26XMZk1w.png) ](</@davidmezzetti?source=post_page---byline--f3456977cf91--------------------------------------->)

[David Mezzetti](</@davidmezzetti?source=post_page---byline--f3456977cf91--------------------------------------->)

6 min read

·

Aug 14, 2024

\--

Listen

Share

More

Press enter or click to view image in full size

Retrieval Augmented Generation (RAG) helps reduce the risk of hallucinations by limiting the context in which a Large Language Model (LLM) can generate answers. This is typically done with a search query that hydrates a prompt with a relevant context.

This article introduces an open-source RAG application built on top of [txtai](<https://github.com/neuml/txtai>), an all-in-one embeddings database for semantic search, LLM orchestration and language model workflows. If you’re not familiar with RAG, see the following links for a general introduction.

## [Getting started with RAGLearn about the methods available to ground LLMs with factsmedium.com](</neuml/getting-started-with-rag-9a0cca75f748?source=post_page-----f3456977cf91--------------------------------------->)

## [Vector Search & RAG Landscape: A review with txtaiEvaluating txtai against popular open-source alternativesmedium.com](</neuml/vector-search-rag-landscape-a-review-with-txtai-a7f37ad0e187?source=post_page-----f3456977cf91--------------------------------------->)

The two primary ways to run this application are as a Docker container and with a Python virtual environment. [See the GitHub repository](<https://github.com/neuml/rag?tab=readme-ov-file#quickstart>) for detailed install instructions.

## Components of a RAG system

Before we describe this application, let’s cover more background on why we even use RAG in the first place? Why not just insert an entire document into a LLM prompt and ask questions? Well the main issue is the context size of LLMs. For a single document, this works but what if you have 100s or 1000s of documents? It’s not only impractical, it’s cost prohibitive.

RAG inserts knowledge into an existing LLM. This starts with a knowledge base / data store containing customized information. This can be a directory of files, a SQL database and/or an API service. When a user asks a question, a context is built using a subset of the best matches. This context then hydrates a LLM prompt and that prompt provides the final answer.

The main components necessary for a RAG system are:

* Text extraction
* Embeddings storage
* Retrieval
* LLM inference

We’ll discuss each of these components over the course of this article.

## RAG with txtai

Press enter or click to view image in full size

The short video clip above gives a brief overview on this RAG system. This application supports two categories of RAG.

* Vector RAG: Context supplied via a vector search query
* Graph RAG: Context supplied via a graph path traversal query

The video above shows a basic vector RAG query. It also shows a Graph RAG query with uploaded data. Let’s talk about how each of these work.

## Vector RAG

Traditional RAG or vector RAG runs a vector search to find the top N most relevant matches to a user’s input. Those matches are passed to an LLM and the answer is returned.

The query `Who created Linux?` runs a vector search for the best matching documents in the Embeddings index. Those matches are then placed into a LLM prompt. The LLM prompt is executed and the answer is returned.

## Graph RAG

Press enter or click to view image in full size

Graph RAG is a new method that uses knowledge or semantic graphs to generate a context. Instead of a vector search, graph path queries are run. Graph RAG in the context of this application supports the following methods to generate context.

* Graph query with the `gq:` prefix. This is a form of graph query expansion. It starts with a vector search to find the top n results. Those results are then expanded using a graph network stored alongside the vector database.
`gq: Tell me about Linux`
* Graph path query. This query takes a list of concepts and finds the nodes that match closest to those concepts. A graph path traversal then runs to build a context of nodes related to those concepts. The result of this traversal is passed to the LLM as the context.
`linux -> macos -> microsoft windows`
* Combination of both. This first runs a graph path query then runs a graph query only within the context of that path traversal.
`linux -> macos -> microsoft windows gq: Tell me about Linux`

Every Graph RAG query response will also show a corresponding graph to help understand how the query works. Each node in the graph is a section (paragraph). The node nodes are generated with a LLM prompt that applies a topic label at upload time.

## Adding data to the index

Regardless of whether the RAG application was a new Embeddings index or an existing one, additional data can be added.

Data can be added as follows.

Press enter or click to view image in full size

When a query begins with a `#` the URL or file is read by the RAG application and loaded into the index. This method also supports loading text directly into the index. For example `# txtai is an all-in-one embeddings database` would create a new entry in the Embeddings database.

## Text Extraction Workflow

Press enter or click to view image in full size

The majority of this application works the same regardless of the data. Once content is loaded into an Embeddings index, the retrieval and LLM generation runs in a consistent way.

The most crucial aspect of this application is being able to transform and load your own data into an Embeddings database. This is the `text extraction` process, which is done via the [Textractor pipeline](<https://neuml.github.io/txtai/pipeline/data/textractor/>).

The Textractor pipeline extracts and splits text from documents. This pipeline uses [Apache Tika](<https://github.com/chrismattmann/tika-python>) (if Java is available) and [BeautifulSoup4](<https://pypi.org/project/beautifulsoup4/>). See [this link](<https://tika.apache.org/2.9.2/formats.html>) for a list of supported document formats.

Each document goes through the following process.

* Content is retrieved if it’s not local
* If the document `mime-type` isn't plain text or HTML, it's run through Tika and converted to XHTML
* XHTML is converted to Markdown and returned

Without Apache Tika, this pipeline only supports plain text and HTML. Other document types require Tika and Java to be installed. Another option is to start Apache Tika via [this Docker Image](<https://hub.docker.com/r/apache/tika>).

While retrieval and prompt engineering are important components of a RAG pipeline, Markdown gives an additional boost. It’s been shown [[1](<https://arxiv.org/abs/2401.10225>)][[2](<https://arxiv.org/abs/2402.12869>)] that Markdown formatted text helps improve RAG accuracy as LLMs have an understanding of formatting elements like tables, lists and emphasized text.

## HTML Article Extraction

Press enter or click to view image in full size

Most websites have headers, footers and sidebars with little consistency. Just naively converting a website to Markdown leads to a lot of irrelevant content which can throw LLMs off.

The Textractor pipeline has logic to detect the most likely sections with the main content ignoring noisy sections such as headers, footers and sidebars. It also supports generating tables, lists, code, blockquotes and emphasis sections as Markdown.

Check out this example extraction:

[Raw Text](<https://gist.githubusercontent.com/davidmezzetti/d22b62a8dceeb210b8b0803f9fd42729/raw/7fc6e912d29506ea3c49d9cc94aa11cec6bcaf50>) | [Formatted Text](<https://gist.github.com/davidmezzetti/d22b62a8dceeb210b8b0803f9fd42729>)

See how only the main content is extracted and the main section formatting is preserved.

For example, the section headers, code blocks and links are preserved.

Press enter or click to view image in full size

And lists are converted to Markdown elements.

Press enter or click to view image in full size

## Wrapping Up

This article covered an easy-to-use RAG application with txtai. This application can be started with a single Docker command and enables quickly exploring your own custom data. Go ahead and give it a try!
