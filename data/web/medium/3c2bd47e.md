---
domain: medium.com
fetch_date: '2026-05-18T12:53:38.299820'
status: ok
url: https://medium.com/@princekrampah/real-time-llm-voice-chat-in-python-kokoro-moonshine-open-source-models-6c6270cbe967
---

# Real Time LLM Voice Chat In Python | Kokoro & MoonShine Open Source Models

[ ![Prince Krampah](https://miro.medium.com/v2/resize:fill:64:64/1*K0tJZ-nblOhECZsmmoTuUw.jpeg) ](</@princekrampah?source=post_page---byline--6c6270cbe967--------------------------------------->)

[Prince Krampah](</@princekrampah?source=post_page---byline--6c6270cbe967--------------------------------------->)

10 min read

·

Apr 8, 2025

\--

Listen

Share

More

Hello guys, welcome back to another exciting article series. In this article, we’ll start a deep dive on real-time streaming in LLM using Python. For so long I wished I was able to do real-time voice streaming in my Python application using my own LLM. It has been really difficult trying to get the code working fine and running. With some research and perseverance, I finally found a beautiful Python package to help me do just that. FastRTC is a Python package to help with real-time communication in Python.

Using FastRTC I was able to do what I wanted to do. In this article, we’ll tackle real-time voice streaming in Python both in Gradio and FastAPI using FastRTC. Let’s get started.

Press enter or click to view image in full size

Image By Code With Prince

## Project Setup

create a project directory called `fastrtc_python` or whatever name you wish to use.

```bash mkdir fastrtc_pythoncd fastrtc_python ```

Once this is done, we can now move on ahead and create a virtual environment for our project.

``` uv venv .venv ```

I used `uv` in this case, if you do not know what `uv` is, kindly refer to this [docs](<https://docs.astral.sh/uv/guides/install-python/>).

We now need to activate our virtual environment.

``` source .venv/bin/activate ```

Once done, head over to the official documentation of FastRTC at <https://fastrtc.org/> and copy the installation command.

```python uv pip install "fastrtc[vad, stt, tts]" ```

Press enter or click to view image in full size

Image By Code With Prince

Once the installation is done, we are set to go!

I went on ahead and created a folder called `basics` , inside this folder, I have created a `lesson_01.py` file:

Press enter or click to view image in full size

Image By Code With Prince

## Simple Demo App | Voice Echo App

To test everything out, I simply created the voice echo app on the official website. We’ll use this to test and make sure all is running well.

Here is the code for this app:

```python from fastrtc import Stream, ReplyOnPauseimport numpy as npdef echo(audio: tuple[int, np.ndarray]): yield audiostream = Stream( handler=ReplyOnPause(echo), modality="audio", mode="send-receive",)stream.ui.launch() ```

Head over to your terminal and use this command to run the application

``` uv run python3 lesson_01.py ```

This will start the Gradio app running on <http://127.0.0.1:7860/>

Press enter or click to view image in full size

Image By Code With Prince

Press enter or click to view image in full size

Image By Code With Prince

Make sure to allow your microphone permissions to be used by the browser. Once this is done, you can record your voice and you’ll hear it being echoed back to you!

## Audio Streaming With Reply on Pause

The whole concept of Reply On Pause is to let the AI start speaking once the user has stopped talking. This is done using Voice Activity Detection, which you’ll commonly see referred to as VAD. Just to bring it to your attention, remember when we were installing the FastRTC package? We used this command:

```python uv pip install "fastrtc[vad, stt, tts]" ```

Now, you can see the `vad` part, voice activity detection. Yes, you got it. We'll use this to detect if the user has stopped speaking and then reply on any pause. We can configure this to control many things like how long the pause should be.

We’ll come back to this just in a second.

## Groq API Key

We’ll be using Groq as the LLM provider in this case. You’ll have to get an API key for Groq to follow along, you can do this by following this link (the link seems to be missing from your message).

Once there, click on the API Key option in the navigation bar, create a new API key and copy it, make sure to copy it into your clipboard. Once copied, go back into your code editor and create a file called `.env` and paste in the copied API key following this pattern:

``` GROQ_API_KEY=<your_api_key_goes_here> ```

Go back to [this page](<https://console.groq.com/docs/quickstart>) and copy the installation command to install Groq in our virtual environment.

```python uv pip install groq ```

## ElevenLabs API Key

We’ll be using ElevenLabs in this article, you can also use other AI voice providers if you want to. Later on, maybe in another article, we can go over how to use open source models like [Kokoro](<https://huggingface.co/hexgrad/Kokoro-82M>).

You first need to sign up for [ElevenLabs](<https://elevenlabs.io/app/conversational-ai>)

Press enter or click to view image in full size

Image By Code With Prince

Once you sign up, you are given some free tokens to play around with. Let’s first get our API key. Click on the API key option to copy it and add it to your `.env` file in the following format:

``` ELEVENLABS_API_KEY=<your_api_key_goes_here> ```

We also need to install the ElevenLabs package in Python.

```python uv pip install elevenlabs ```

## Loading Environment Variables

Once done, we’ll have to load in our environment variables. I will use the `python-dotenv` package and the `os` module for this.

```python uv pip install python-dotenv ```

## Transcriping Audio And Generating Text

The first step we’ll take is to transcribe the audio and generate the needed text. This text will be passed to our LLM to generate a text response.

Here is the code for this, don’t worry about the other inputs not being used in this snippet, we’ll use them in a short while:

```python from fastrtc import ReplyOnPause, Stream, get_stt_model, AdditionalOutputsimport numpy as npfrom numpy.typing import NDArrayfrom dotenv import load_dotenvfrom groq import Groqfrom elevenlabs import ElevenLabsfrom fastapi import FastAPIimport gradio as grimport osimport timeload_dotenv()groq_client = Groq(api_key=os.getenv("GROQ_API_KEY"))tts_client = ElevenLabs(api_key=os.getenv("ELEVENLABS_API_KEY"))stt_model = get_stt_model()def generate_response( audio: tuple[int, NDArray[np.int16 | np.float32]], chatbot: list[dict] | None = None,): chatbot = chatbot or [] messages = [{"role": msg["role"], "content": msg["content"]} for msg in chatbot] start = time.time() text = stt_model.stt(audio) print("transcription", time.time() - start) print("prompt", text) chatbot.append({"role": "user", "content": text}) yield AdditionalOutputs(chatbot) messages.append({"role": "user", "content": text}) response_text = ( groq_client.chat.completions.create( model="llama-3.1-8b-instant", max_tokens=512, messages=messages, ) .choices[0] .message.content ) chatbot.append({"role": "assistant", "content": response_text}) ```

## Generating Audio Responses With ElevenLabs

Now that we have the text response from the LLM (Groq), we’ll use this to generate a final audio that we’ll send back to the UI.

```python def response( audio: tuple[int, NDArray[np.int16 | np.float32]], chatbot: list[dict] | None = None): # Transcription and response generation gen = generate_response(audio, chatbot) # First yield is AdditionalOutputs with updated chatbot chatbot =next(gen) # Second yield is the response text response_text = next(gen) print(response_text) # Text-to-speech for chunk in tts_client.text_to_speech.convert_as_stream( text=response_text, voice_id="JBFqnCBsd6RMkjVDRZzb", model_id="eleven_multilingual_v2", output_format="pcm_24000", ): audio_array = np.frombuffer(chunk, dtype=np.int16).reshape(1, -1) yield (24000, audio_array) yield AdditionalOutputs(chatbot) ```

## Running The Code

To run the code, I have implemented this logic in place,

```json stream = Stream( handler=ReplyOnPause(response, input_sample_rate=16000), modality="audio", mode="send-receive", ui_args={ "title": "LLM Voice Chat (Powered by Groq, ElevenLabs, and WebRTC ⚡️)"},)stream.ui.launch() ```

Once the file is saved, you can go in your terminal and run it using this command:

``` uv run python3 lesson_02.py ```

This will start the application on <http://127.0.0.1:7860/>

Press enter or click to view image in full size

Image By Code With Prince

## Transciption With Groq And Text To Voice With ElevenLabs

I have modified the code to do the Speech To Text With Groq And Text To Speech With ElevenLabs

```python from fastrtc import ReplyOnPause, Stream, AdditionalOutputsimport numpy as npfrom numpy.typing import NDArrayfrom dotenv import load_dotenvfrom groq import Groqfrom elevenlabs import ElevenLabsimport osimport timeimport tempfileimport waveload_dotenv()groq_client = Groq(api_key=os.getenv("GROQ_API_KEY"))tts_client = ElevenLabs(api_key=os.getenv("ELEVENLABS_API_KEY"))def audio_to_wav_file(audio_data, sample_rate): """Convert audio data to a temporary WAV file for Groq processing""" temp_file = tempfile.NamedTemporaryFile(suffix=".wav", delete=False) with wave.open(temp_file.name, 'wb') as wf: wf.setnchannels(1) wf.setsampwidth(2) # 16-bit audio wf.setframerate(sample_rate) wf.writeframes(audio_data.tobytes()) return temp_file.namedef transcribe_with_groq(audio): """Transcribe audio using Groq's Whisper API""" sample_rate, audio_data = audio # Convert audio data to a temporary WAV file wav_file = audio_to_wav_file(audio_data, sample_rate) try: with open(wav_file, "rb") as file: translation = groq_client.audio.translations.create( file=(wav_file, file.read()), model="whisper-large-v3", response_format="json", temperature=0.0 ) return translation.text finally: # Clean up the temporary file if os.path.exists(wav_file): os.remove(wav_file)def generate_response( audio: tuple[int, NDArray[np.int16 | np.float32]], chatbot: list[dict] | None = None): chatbot = chatbot or [] messages = [{"role": msg["role"], "content": msg["content"]} for msg in chatbot] start = time.time() text = transcribe_with_groq(audio) print("transcription", time.time() - start) print("prompt", text) chatbot.append({"role": "user", "content": text}) yield AdditionalOutputs(chatbot) messages.append({"role": "user", "content": text}) response_text = ( groq_client.chat.completions.create( model="llama-3.1-8b-instant", max_tokens=512, messages=messages, ) .choices[0] .message.content ) chatbot.append({"role": "assistant", "content": response_text}) yield response_textdef response( audio: tuple[int, NDArray[np.int16 | np.float32]], chatbot: list[dict] | None = None): # Transcription and response generation gen = generate_response(audio, chatbot) # First yield is AdditionalOutputs with updated chatbot chatbot = next(gen) # Second yield is the response text response_text = next(gen) print(response_text) # Text-to-speech using ElevenLabs for chunk in tts_client.text_to_speech.convert_as_stream( text=response_text, voice_id="JBFqnCBsd6RMkjVDRZzb", model_id="eleven_multilingual_v2", output_format="pcm_24000", ): audio_array = np.frombuffer(chunk, dtype=np.int16).reshape(1, -1) yield (24000, audio_array)stream = Stream( handler=ReplyOnPause(response, input_sample_rate=16000), modality="audio", mode="send-receive", ui_args={ "title": "LLM Voice Chat (Powered by Groq, ElevenLabs, and WebRTC ⚡️)"},)stream.ui.launch() ```

## Using Groq For TTS and STT

```python from fastrtc import ReplyOnPause, Stream, AdditionalOutputsimport numpy as npfrom numpy.typing import NDArrayfrom dotenv import load_dotenvfrom groq import Groqfrom elevenlabs import ElevenLabsimport osimport timeimport tempfileimport waveload_dotenv()groq_client = Groq(api_key=os.getenv("GROQ_API_KEY"))tts_client = ElevenLabs(api_key=os.getenv("ELEVENLABS_API_KEY"))def audio_to_wav_file(audio_data, sample_rate): """Convert audio data to a temporary WAV file for Groq processing""" temp_file = tempfile.NamedTemporaryFile(suffix=".wav", delete=False) with wave.open(temp_file.name, 'wb') as wf: wf.setnchannels(1) wf.setsampwidth(2) # 16-bit audio wf.setframerate(sample_rate) wf.writeframes(audio_data.tobytes()) return temp_file.namedef transcribe_with_groq(audio): """Transcribe audio using Groq's Whisper API""" sample_rate, audio_data = audio # Convert audio data to a temporary WAV file wav_file = audio_to_wav_file(audio_data, sample_rate) try: with open(wav_file, "rb") as file: translation = groq_client.audio.translations.create( file=(wav_file, file.read()), model="whisper-large-v3", response_format="json", temperature=0.0 ) return translation.text finally: # Clean up the temporary file if os.path.exists(wav_file): os.remove(wav_file)def generate_response( audio: tuple[int, NDArray[np.int16 | np.float32]], chatbot: list[dict] | None = None): chatbot = chatbot or [] messages = [{"role": msg["role"], "content": msg["content"]} for msg in chatbot] start = time.time() text = transcribe_with_groq(audio) print("transcription", time.time() - start) print("prompt", text) chatbot.append({"role": "user", "content": text}) yield AdditionalOutputs(chatbot) messages.append({"role": "user", "content": text}) response_text = ( groq_client.chat.completions.create( model="llama-3.1-8b-instant", max_tokens=512, messages=messages, ) .choices[0] .message.content ) chatbot.append({"role": "assistant", "content": response_text}) yield response_textdef response( audio: tuple[int, NDArray[np.int16 | np.float32]], chatbot: list[dict] | None = None): # Transcription and response generation gen = generate_response(audio, chatbot) # First yield is AdditionalOutputs with updated chatbot chatbot = next(gen) # Second yield is the response text response_text = next(gen) print(response_text) # Text-to-speech using Groq print("Using Groq for text-to-speech") tts_response = groq_client.audio.speech.create( model="playai-tts", voice="Fritz-PlayAI", input=response_text, response_format="wav" ) # Create a temporary file to store the audio temp_file = tempfile.NamedTemporaryFile(suffix=".wav", delete=False) temp_file_path = temp_file.name temp_file.close() # Write the audio to the temporary file tts_response.write_to_file(temp_file_path) # Read the audio file with wave.open(temp_file_path, 'rb') as wf: sample_rate = wf.getframerate() n_frames = wf.getnframes() audio_data = wf.readframes(n_frames) # Clean up the temporary file os.remove(temp_file_path) # Convert to numpy array audio_array = np.frombuffer(audio_data, dtype=np.int16).reshape(1, -1) yield (sample_rate, audio_array)stream = Stream( handler=ReplyOnPause(response, input_sample_rate=16000), modality="audio", mode="send-receive", ui_args={ "title": "LLM Voice Chat (Powered by Groq, ElevenLabs, and WebRTC ⚡️)"},)stream.ui.launch() ```

## Fully Localized TTS And STT Implementation

```python from fastrtc import ( ReplyOnPause, Stream, AdditionalOutputs, get_stt_model, get_tts_model, KokoroTTSOptions)import numpy as npfrom numpy.typing import NDArrayfrom dotenv import load_dotenvfrom groq import Groqimport osimport timeimport tempfileimport waveload_dotenv()groq_client = Groq(api_key=os.getenv("GROQ_API_KEY"))tts_client = get_tts_model(model="kokoro")stt_model = get_stt_model(model="moonshine/base")options = KokoroTTSOptions( voice="af_heart", speed=1.0, lang="en-us")def generate_response( audio: tuple[int, NDArray[np.int16 | np.float32]], chatbot: list[dict] | None = None): chatbot = chatbot or [] messages = [{"role": msg["role"], "content": msg["content"]} for msg in chatbot] start = time.time() # Use local STT model instead of Groq's Whisper text = stt_model.stt(audio) print("transcription", time.time() - start) print("prompt", text) chatbot.append({"role": "user", "content": text}) yield AdditionalOutputs(chatbot) messages.append({"role": "user", "content": text}) response_text = ( groq_client.chat.completions.create( model="llama-3.1-8b-instant", max_tokens=512, messages=messages, ) .choices[0] .message.content ) chatbot.append({"role": "assistant", "content": response_text}) yield response_textdef response( audio: tuple[int, NDArray[np.int16 | np.float32]], chatbot: list[dict] | None = None): # Transcription and response generation gen = generate_response(audio, chatbot) # First yield is AdditionalOutputs with updated chatbot chatbot = next(gen) # Second yield is the response text response_text = next(gen) print(response_text) # Use tts_client.stream_tts_sync for TTS (Local TTS) for chunk in tts_client.stream_tts_sync(response_text): yield chunkstream = Stream( handler=ReplyOnPause(response, input_sample_rate=16000), modality="audio", mode="send-receive", ui_args={ "title": "LLM Voice Chat (Powered by Groq, Kokoro, and Moonshine ⚡️)"},)stream.ui.launch() ```

## GitHub

## [GitHub - Princekrampah/real-time-llm-voice-chat-systemsContribute to Princekrampah/real-time-llm-voice-chat-systems development by creating an account on GitHub.github.com](<https://github.com/Princekrampah/real-time-llm-voice-chat-systems?source=post_page-----6c6270cbe967--------------------------------------->)

## Conclusion

Congratulations on making it this far! Throughout this article, we’ve explored the exciting world of real-time voice streaming in Python using FastRTC. We’ve learned how to build a system that can listen to user input, transcribe it using Whisper, generate intelligent responses with Groq’s LLM, and convert those responses back to speech using ElevenLabs.

The Reply On Pause feature we implemented demonstrates how Voice Activity Detection (VAD) can create more natural conversational flows between humans and AI. By detecting when a user stops speaking and responding appropriately, we’ve created a more engaging and intuitive user experience.

We’ve covered everything from setting up your environment and API keys to implementing the core functionality in both Gradio and FastAPI. This foundation will enable you to build sophisticated voice-based applications that can understand, process, and respond to human speech in real-time.

Whether you’re building a voice assistant, a customer service bot, or exploring new ways for humans to interact with AI, the techniques we’ve covered provide a solid starting point for your projects.

**Other platforms where you can reach out to me:**

1. [YouTube](<http://www.youtube.com/c/codewithprince>)
2. [Twitter](<https://twitter.com/prince_krampah>)
3. [LinkedIn](<https://www.linkedin.com/in/prince-krampah-5a2b921bb/?originalSubdomain=tz>)
4. [Discord](<https://discord.gg/KAdxykMS>)

** _Happy coding! And see you next time, the world keeps spinning._**
